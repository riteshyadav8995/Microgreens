# Contact form delivery via FormSubmit (https://formsubmit.co) — free, no signup, no backend.
# Put the Gmail address that should receive "Contact Us" messages.
# The FIRST submission sends an activation email to this inbox — click "Activate Form" once.
# After activation FormSubmit also gives you a random alias string; you can use that here
# instead of the raw email to keep the address out of the page source.
VITE_FORMSUBMIT_EMAIL=rk5061288@gmail.com
